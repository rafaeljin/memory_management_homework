非常基础的实现first fit, best fit 和worst fit的实现，不考虑对其，也不对释放的内存作检查。
每小块使用一个含2个数字的pair表示，分别表示起始地址和大小；可使用内存使用一个这样的列表表示；
每个函数有基础的说明；
使用的例子是 init free = 1000 
	malloc 50,300,50,200,50
	free 2nd(size=300), 4th(size=200)
	malloc 100  
